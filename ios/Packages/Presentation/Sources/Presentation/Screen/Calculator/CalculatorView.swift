import SwiftUI

/// 계산기 화면 (2단계에서 구현 예정)
struct CalculatorView: View {
    var body: some View {
        ContentUnavailableView(
            "계산기",
            systemImage: "plus.forwardslash.minus",
            description: Text("2단계에서 구현됩니다"),
        )
    }
}
